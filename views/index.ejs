<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <% if (locals.csrf_token) { %>
      <meta name="csrf-token" content="<%= csrf_token %>">
    <% } %>
    <title>TypeScript React Starter</title>
  </head>
  <body>
    <div id="app"></div>
    <script src="/main.bundle.js"></script>
  </body>
</html>
